$(document).ready(function () {
    var paragraphs = $('p');
    // var newStyle = function () {
    //     paragraphs.css("color", "red");
    // }
    $(paragraphs).css({
        'background-color': 'aqua',
        'font-size': '1.4rem',
        'color': 'blue',
    });
});
